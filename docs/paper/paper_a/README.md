# Paper A (draft)

Working title: **An operational reconstruction ladder for spacetime quantities
from causal order.**

Status: DRAFT v0.2 (2026-07-09). The scientific content is the verified M1-M10
foundation layer on the `restart/m17-baseline` line, whose numerical
correctness against known causal-set and special-relativistic results was
independently checked. Every quantitative result is grounded in the experiment
output CSVs (`outputs/data/`, regenerable); no number is from memory. Figures
are the natural next addition (a ladder schematic and/or grounded convergence
panels).

## Scope and relation to Paper B

Paper A is the **reconstruction ladder**: given a causal (accessibility) order
plus a declared, minimal set of supplied ingredients — a counting measure, an
observer chain with clock labels, an orientation reference, an observer atlas,
supplied conformal weights — which spacetime quantities can be operationally
reconstructed, and which cannot. It re-derives standard causal-set and
radar-protocol results as controlled validations and, as its own contribution,
makes explicit the accounting of what each rung requires and the negative
results that bound it.

Paper B (separate) takes the next question — whether observer-relative distance
*order* can be validated as recovering latent geometry — and builds a
preregistered discriminator for it. Paper A is the foundation Paper B stands on.

## Files

- `manuscript.md` — the v0.1 draft (all sections).
- `claim_boundary.md` — claim/non-claim checklist.
- `citations/references.bib` — verified bibliography (shared core with Paper B;
  each entry confirmed against an authoritative source).
- `figures/` — figure-generation script + PNGs (to be added).

## Provenance

Foundation-layer commit: M17 baseline `325df55`. Experiment outputs are
regenerated by running `experiments/exp0*.py` / `exp1*.py` with
`PYTHONPATH=src`; the results section cites the producing script and summary
CSV for each number.
